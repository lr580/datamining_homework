#include <bits/stdc++.h>
using namespace std;
struct DSU {
    vector<int> fa;
    DSU(int n) {
        fa.resize(n);
        iota(fa.begin(), fa.end(), 0); // fa[i]=i

    }
};
signed main(int argc, char*argv[]) {

    return 0;
}